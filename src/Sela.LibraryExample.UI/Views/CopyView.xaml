<Window
  x:Class="Sela.LibraryExample.UI.Views.CopyView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:helpers="clr-namespace:Sela.LibraryExample.UI.Helpers"
  xmlns:local="clr-namespace:Sela.LibraryExample.UI.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  SizeToContent="WidthAndHeight"
  Title="CopyView"
  mc:Ignorable="d">
  <Window.Resources>
    <helpers:IsbnToTitleConverter x:Key="IsbnToTitleConverter"/>
  </Window.Resources>
  <StackPanel>
    <Label x:Name="Title" Content="{Binding Copy.ISBN, Converter={StaticResource IsbnToTitleConverter}, Mode=OneWay}"/>
    <Label x:Name="Copy">
      <TextBlock>
        <Run Text="Copy #"/>
        <Run Text="{Binding Copy.CopyNumber, Mode=OneWay}"/>
      </TextBlock>
    </Label>
    <Label x:Name="LentTo">
      <TextBlock>
        <Run Text="Lent to:"/>
        <Run Text="{Binding Copy.LentTo, Mode=OneWay}"/>
      </TextBlock>
    </Label>
    <StackPanel Orientation="Horizontal">
      <Button
        x:Name="LendButton"
        Command="{Binding LendCopyCommand}"
        CommandParameter="{Binding}"
        Content="Lend Copy"/>
      <Button
        x:Name="ReturnButton"
        Command="{Binding ReturnCopyCommand}"
        Content="Return Copy"/>
    </StackPanel>
  </StackPanel>
</Window>