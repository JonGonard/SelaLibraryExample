<Window x:Class="Sela.LibraryExample.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
  <StackPanel x:Name="MainPanel">
    <ListView x:Name="CatalogList" ItemsSource="{Binding Library}" IsSynchronizedWithCurrentItem="True">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="ISBN" DisplayMemberBinding="{Binding ISBN}" />
          <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" />
          <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding Genre}" />
        </GridView>
      </ListView.View>
    </ListView>
    <StackPanel x:Name="OptionsPanel" Orientation="Horizontal">
      <Button x:Name="AddNewItemButton" Content="Add" Command="{Binding AddNewItemCommand}"/>
      <Button x:Name="RemoveItemButton" Content="Delete" Command="{Binding RemoveItemCommand}" CommandParameter="{Binding Library/}"/>
    </StackPanel>
  </StackPanel>
</Window>
